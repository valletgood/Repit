# Repit - 운동 기록 & 분석 앱

<p align="center">
  <img src="public/images/logo.svg" alt="Repit Logo" width="200" />
</p>

> 당신의 운동을 기록하고, 분석하고, 성장을 추적하세요.

## 🌐 배포

**Live Demo**: [https://repit-three.vercel.app](https://repit-three.vercel.app)

> 이 프로젝트는 **Vibe Coding**으로 개발되었습니다. AI 어시스턴트와 함께 대화하며 코드를 작성하는 새로운 개발 방식을 적용했습니다.
> 
>
> **Repit은 모바일 웹 환경에 최적화된 서비스입니다.**
> 데스크톱보다 **모바일 기기에서 접속**하거나,  
> **홈 화면에 추가(PWA 설치)** 하여 앱처럼 사용하는 것을 권장합니다.

## 📱 주요 기능

### ✅ 현재 구현된 기능

| 기능 | 설명 |
|------|------|
| **회원가입/로그인** | 토큰 기반 인증 (Access Token + Refresh Token) |
| **루틴 관리** | 나만의 운동 루틴 생성 및 관리 |
| **운동 기록** | 루틴 기반 운동 수행 및 세트별 기록 |
| **캘린더 기록** | 날짜별 운동 기록 조회, 월별 운동 일수 표시 |
| **주간 차트** | 최근 7일간 운동 시간 바 차트 |
| **마이페이지** | 사용자 정보 확인 및 로그아웃 |
| **PWA 지원** | 모바일 앱처럼 설치 가능 |

### 🚧 개발 예정 기능

#### 차트 페이지 확장
| 차트 | 설명 |
|------|------|
| **부위별 중량 차트** | 가슴, 등, 어깨, 하체 등 부위별 총 중량 추이 |
| **부위별 수행 비율** | 파이 차트로 어떤 부위를 더 많이 운동했는지 시각화 |

## 🛠 기술 스택

### Frontend
- **Framework**: Next.js 15+ (App Router)
- **Language**: TypeScript
- **Styling**: Tailwind CSS 4
- **UI Components**: shadcn/ui, Radix UI
- **State Management**: Redux Toolkit + redux-persist
- **Data Fetching**: TanStack Query (React Query)
- **HTTP Client**: Axios
- **Charts**: Recharts
- **Calendar**: react-calendar
- **Icons**: Lucide React

### Backend
- **API**: Next.js API Routes
- **Database**: PostgreSQL (Supabase)
- **ORM**: Drizzle ORM
- **Authentication**: Custom Token (HttpOnly Cookies)

### DevOps
- **Package Manager**: pnpm
- **Linting**: ESLint
- **Formatting**: Prettier

## 📂 프로젝트 구조

```
src/
├── app/
│   ├── (auth)/                 # 인증 페이지 (로그인, 회원가입)
│   │   ├── login/
│   │   └── register/
│   ├── (main)/                 # 메인 앱 (하단 네비게이션)
│   │   ├── _components/        # 페이지 전용 컴포넌트
│   │   ├── page.tsx            # 홈 (루틴 목록)
│   │   ├── record/             # 운동 기록 (캘린더)
│   │   ├── chart/              # 통계 차트
│   │   ├── mypage/             # 마이페이지
│   │   ├── doing/[id]/         # 운동 진행
│   │   └── reg-routine/        # 루틴 등록
│   └── api/                    # API Routes
│       ├── auth/               # 인증 API
│       │   ├── login/
│       │   ├── logout/
│       │   ├── register/
│       │   └── check-id/
│       └── main/               # 메인 기능 API
│           ├── record/
│           ├── chart/
│           ├── doing/
│           ├── exercises/
│           └── reg-routine/
├── components/
│   ├── ui/                     # shadcn/ui 컴포넌트
│   ├── chart/                  # 차트 컴포넌트
│   ├── navigation/             # 네비게이션
│   ├── guards/                 # 인증 가드
│   └── providers/              # Provider 컴포넌트
├── db/
│   ├── index.ts                # Drizzle DB 인스턴스
│   └── schema.ts               # 테이블 스키마
├── lib/
│   ├── axios.ts                # Axios 인스턴스
│   ├── auth.ts                 # 인증 유틸리티
│   └── utils.ts                # 공통 유틸리티
├── redux/
│   ├── configStore.ts          # Redux Store 설정
│   ├── hooks.ts                # 커스텀 훅
│   └── slices/                 # Redux Slices
├── hooks/                      # 공통 커스텀 훅
└── types/                      # 타입 정의
```

## 🗄 데이터베이스 스키마

| 테이블 | 설명 |
|--------|------|
| `users` | 사용자 정보 |
| `exercises` | 운동 종류 (벤치프레스, 스쿼트 등) |
| `workout_sessions` | 운동 세션 (하루 운동 기록) |
| `workout_sets` | 운동 세트 (세트별 무게, 횟수, 시간) |
| `routines` | 사용자 루틴 |
| `routine_exercises` | 루틴에 포함된 운동 |
| `routine_exercise_sets` | 루틴 운동의 기본 세트 설정 |
| `refresh_tokens` | 리프레시 토큰 |

## 🚀 시작하기

### 1. 의존성 설치

```bash
pnpm install
```

### 2. 환경 변수 설정

`.env.local` 파일 생성:

```env
DATABASE_URL=postgresql://...
NEXT_PUBLIC_API_URL=http://localhost:3000
```

### 3. 데이터베이스 설정

```bash
# 스키마를 데이터베이스에 푸시
pnpm db:push

# 운동 데이터 시드
pnpm db:seed
```

### 4. 개발 서버 실행

```bash
pnpm dev
```

## 📜 스크립트

| 명령어 | 설명 |
|--------|------|
| `pnpm dev` | 개발 서버 실행 |
| `pnpm build` | 프로덕션 빌드 |
| `pnpm start` | 프로덕션 서버 실행 |
| `pnpm lint` | ESLint 실행 |
| `pnpm format` | Prettier 포맷팅 |
| `pnpm db:generate` | Drizzle 마이그레이션 생성 |
| `pnpm db:push` | 스키마를 DB에 직접 푸시 |
| `pnpm db:studio` | Drizzle Studio 실행 |
| `pnpm db:seed` | 운동 데이터 시드 |

## 📄 라이선스

MIT
